{% extends 'base.html.twig' %}
{% block title %}Новий аккаунт{% endblock %}
{% block body %}
    <div style="text-align: center;">
        <div style="width: 45%; display: inline-block;">
            <p class="text-title">Створення нового аккаунта</p>
            <p class="text-subtitle">Укажіть назву, опис (по бажанню) та<br>грошову одиницю</p>

            <div class="col-md-9" style="margin: 0 auto;">
                <div class="panel-body">

                    {{ form_start(form) }}

                    <div class="container-form">
                        {{ form_row(form.name, {'attr': {'class': 'form-control'}}) }}
                    </div>

                    <div class="container-form">
                        {{ form_row(form.description, {'attr': {'class': 'form-control'}}) }}
                    </div>

                    <div class="container-form">
                        {{ form_row(form.currency, {'attr': {'class': 'form-control'}}) }}
                    </div>

                    <div class="container-form-center" align="center">
                        <div class="col-md-6">
                            <button class="btn btn-block btn-primary" type="submit">Створити</button>
                        </div>
                    </div>

                    {{ form_end(form) }}

                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% extends 'base.html.twig' %}
{% block title %}Нова категорія{% endblock %}
{% block body %}
    <div style="text-align: center;">
        <div style="width: 45%; display: inline-block;">
            <p class="text-title">Створення нової категорі</p>
            <p class="text-subtitle">Укажіть назву і тип та натисніть кнопку "Створити"</p>

            <div class="col-md-9" style="margin: 0 auto;">
                <div class="panel-body">

                    {{ form_start(form) }}

                    <div class="container-form">
                        {{ form_row(form.name, {'attr': {'class': 'form-control'}}) }}
                    </div>

                    <div class="container-form">
                        {{ form_row(form.type, {'attr': {'class': 'form-control'}}) }}
                    </div>

                    <div class="container-form-center" align="center">
                        <div class="col-md-6">
                            <button class="btn btn-block btn-primary" type="submit">Створити</button>
                        </div>
                    </div>

                    {{ form_end(form) }}

                </div>
            </div>
        </div>
    </div>
{% endblock %}